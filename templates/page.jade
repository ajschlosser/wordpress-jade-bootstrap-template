- get_header();
section(role="main").entry
	- if ( have_posts() ) : while ( have_posts() ) : the_post();
		article(id!="post-<?php the_ID(); ?>" class!="<?php post_class(); ?>")
			header.header
				.row
					.col-lg-12
						h1
							- the_title();
			section.content
				- if (is_page("")) {
					.some-container
						.row
							.col-lg-6
							.col-lg-6
				- } else {
					.article-container
						.row
							.col-lg-3.nav
								- get_template_part("sidenav");
							.col-lg-3.art.left-justify
								- the_content();
				- }
	- endwhile; endif;
- get_template_part("");
- get_sidebar();
- get_footer();